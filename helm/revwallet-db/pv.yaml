apiVersion: v1
kind: PersistentVolume
metadata:
  name: revwallet-db-volume
  labels:
    type: local
    app: revwallet-db
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: /data/postgresql